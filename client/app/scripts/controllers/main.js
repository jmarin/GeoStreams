'use strict';

angular.module('clientApp')
  .controller('MainCtrl', function ($scope) {
    // if ($scope.credentials.user != $scope.credentials.password) {
    // 	$location.path('#/login');
    // }

  });
